import React from 'react'
import {Icon, Tooltip} from "@blueprintjs/core";


export const AnketIkon = props => (
    <Tooltip content={"Anket"}>
        <Icon icon={"confirm"}/>
    </Tooltip>
)

export const IdariKayitIkon = props => (
    <Tooltip content="İdari Kayıt">
        <Icon icon={"confirm"}/>
    </Tooltip>
)
